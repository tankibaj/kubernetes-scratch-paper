# global:
#   image:
#     repository: camptocamp/argocd
#     tag: "v2.3.1_c2c.1"

# controller:
#   resources:
#     requests:
#       cpu: 1000m
#       memory: 512Mi

server:
#  extraArgs:
#    - --insecure
  ingress:
    enabled: true
    annotations:
#       kubernetes.io/ingress.class: alb
#       alb.ingress.kubernetes.io/load-balancer-name: argocd-ingress
#       alb.ingress.kubernetes.io/scheme: internet-facing
#       alb.ingress.kubernetes.io/target-type: ip
#       alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS":443}]'
#       alb.ingress.kubernetes.io/ssl-redirect: '443'
#       alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:eu-central-1:XXXXXXXXXXX:certificate/XXXXXXXXXXXX
#       alb.ingress.kubernetes.io/backend-protocol: HTTPS
#       alb.ingress.kubernetes.io/healthcheck-protocol: HTTPS
    hosts:
     - argocd.local.naim.run
  config:
    url: http://argocd.local.naim.run
    # -- exec.enabled indicates whether the UI exec feature is enabled. It is disabled by default.
    # -- Ref: https://argo-cd.readthedocs.io/en/latest/operator-manual/rbac/#exec-resource
    exec.enabled: "true"

notifications:
  securityContext:
    runAsNonRoot: true
    runAsUser: 999


